services:
  webapp:
    build: .
    restart: unless-stopped
    ports:
      - "8080:8080"
    networks:
      - celsia-net
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

networks:
  celsia-net:
    external: true
